# Build Notes - v2.10.0

## Summary
- Bumped crate version from `2.9.0` to `2.10.0`.
- Cosmetic matrix panel updates:
  - Renamed matrix entry label from `Matrix A` to `Matrix Entry`.
  - Renamed buttons: `Push A` -> `Push`, `Preset A` -> `Preset`.
  - Moved `Push I(n)` controls into the `Matrix Builder` panel.
- Added new vector operator `Toep` (Toeplitz):
  - Backend op `toep` builds a Toeplitz matrix from a vector (`Nx1` or `1xN`) using `T[i,j] = v[|i-j|]`.
  - Wired through API + wasm wrappers.
  - Added GUI `Toep` button in `Vector Operators` and action mapping.
- Updated help documentation for new labels/panel placement and `Toep` behavior.
- Added regression coverage for Toeplitz behavior in backend and API tests.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `65 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
