# Build Notes - v3.0.1

## Summary
- Bumped crate version from `3.0.0` to `3.0.1`.
- Restored selected number-entry panel labels for readability:
  - `DEG/RAD`
  - `pick`
  - `+-`
  - `EXP`
- Restored constants panel labels to symbol-based display (including Greek symbols and scientific notation glyphs).
- Renamed `Other` panel title to `Memory`.
- Updated memory controls layout to a vertical (single-column) arrangement so controls fit cleanly within standard panel width.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifact refreshed: `dist/Post_Fix_Pro.html`.
