# Build Notes - v2.24.0

## Summary
- Bumped crate version from `2.23.0` to `2.24.0`.
- Updated scalar keypad layout behavior:
  - Replaced separate `RAD` and `DEG` buttons with one `RAD/DEG` toggle button.
  - Replaced former `DEG` slot with a `Pick` button.
- Added new pick-by-index operation:
  - `pick_from_stack_index` consumes top stack integer index (`#n`) and replaces top with a copy of that stack line value.
  - Added backend and API/wasm bindings and regression coverage.
- Matrix builder updates from prior request are included in this milestone:
  - Added `hstack`, `vstack`, `hravel`, `vravel`.
  - Updated UI buttons and help docs accordingly.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `80 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
