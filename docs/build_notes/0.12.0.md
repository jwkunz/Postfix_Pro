# Build Notes - v0.12.0

## Summary
- Bumped crate minor version from `0.11.0` to `0.12.0`.
- Updated browser loader path to support non-module wasm-bindgen startup.
- Added optional embedded-wasm boot path (`wasm_base64.js`) for local file launches.
- Updated build script to produce a zip-ready self-contained `dist/` folder.
- Updated wasm build documentation with distribution steps.
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`32` tests total)
- Release build: success

## Distribution Flow Added
- `scripts/build_wasm.sh` now:
  - builds with `wasm-pack --target no-modules`
  - copies generated bindings into `dist/pkg/`
  - refreshes `dist/index.html`
  - generates `dist/wasm_base64.js`
  - writes `dist/README.txt`
- `docs/wasm_build.md` now documents:
  - zip-and-share flow for `dist/`
  - expected folder contents
