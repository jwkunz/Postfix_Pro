# Build Notes - v2.21.0

## Summary
- Bumped crate version from `2.20.0` to `2.21.0`.
- Added `LSTSQ Solve` companion operation next to `Solve A*x=B` in the Matrix Operators panel.
- Implemented backend least-squares solver using Moore-Penrose pseudoinverse:
  - New operation: `solve_lstsq`
  - Behavior: solve `x = A^+ * B` (pseudoinverse-based least squares).
  - Accepts rectangular `A` with row-compatible `B`.
- Added API + wasm binding for `solve_lstsq`.
- Updated help docs with `LSTSQ Solve` description.
- Added backend/API tests for overdetermined least-squares solving.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `75 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
