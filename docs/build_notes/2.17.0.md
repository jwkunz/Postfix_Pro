# Build Notes - v2.17.0

## Summary
- Bumped crate version from `2.16.0` to `2.17.0`.
- Hardened decomposition roadmap coverage with complex-focused validation:
  - Updated decomposition reconstruction tests to use complex-safe matrix multiplication.
  - Added LU reconstruction coverage for a complex matrix (`P*A = L*U`).
  - Added EVD reconstruction coverage for a complex diagonal matrix with no warning path.
  - Extended API decomposition tests to include complex `QR` and `LU` success paths.
- Updated help docs to clarify complex-matrix support for `QR` and `LU`.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
