# Build Notes - v2.14.0

## Summary
- Bumped crate version from `2.13.0` to `2.14.0`.
- Fixed non-responsive statistics buttons in the frontend.
- Root cause:
  - stats buttons (`mean`, `mode`, `std dev`, `variance`, `max`, `min`) existed in HTML,
  - but their actions were missing from `opMap`, so clicks were ignored.
- Added missing frontend action mappings:
  - `mean -> mean`
  - `mode -> mode`
  - `std-dev -> std_dev`
  - `variance -> variance`
  - `max -> max_value`
  - `min -> min_value`

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `69 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
