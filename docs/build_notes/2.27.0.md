# Build Notes - v2.27.0

## Summary
- Bumped crate version from `2.26.0` to `2.27.0`.
- Updated stack panel rendering to a fixed-size viewport driven by the `Visible` control.
- Changed default visible stack frame count from `16` to `4`.
- Added `null` placeholders for empty frames so the stack panel height stays constant as stack contents change.
- Added a stack summary line showing `showing N frames (M hidden)`.
- Updated help documentation to describe fixed frame rendering and `null` placeholders.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
