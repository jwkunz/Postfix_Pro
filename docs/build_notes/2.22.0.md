# Build Notes - v2.22.0

## Summary
- Bumped crate version from `2.21.0` to `2.22.0`.
- Improved UX for advanced matrix solve workflows (requested roadmap polish item #2):
  - `LSTSQ Solve` now returns a status/warning message on success that includes residual norm.
  - Rank-deficient least-squares systems now produce an explicit warning that minimum-norm pseudoinverse solution was returned.
- Updated backend `solve_lstsq` flow to propagate warnings through API response warning channel.
- Updated help documentation for `LSTSQ Solve` warning semantics.
- Added regression tests:
  - Backend: overdetermined solve warning presence + rank-deficiency warning content.
  - API: warning propagation includes residual-norm text.

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `76 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/Post_Fix_Pro.png`, `dist/pkg`, `dist/wasm_base64.js`).
