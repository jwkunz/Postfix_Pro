# Build Notes - v2.8.0

## Summary
- Bumped crate version from `2.7.0` to `2.8.0`.
- Added explicit element-wise matrix binary operations:
  - `HadMul`: Hadamard (element-wise) multiplication for equal-shape matrices.
  - `HadDiv`: element-wise division for equal-shape matrices.
- Preserved existing behavior for `*` as matrix multiplication; Hadamard ops are separate and explicit.
- Wired new operations through:
  - backend calculator core (`src/lib.rs`)
  - API + wasm layer (`src/api.rs`)
  - matrix panel action buttons and frontend op map (`Post_Fix_Pro.html`)
- Updated `help.md` with new matrix operation descriptions.
- Added tests:
  - backend tests for Hadamard multiply/divide
  - API tests for Hadamard endpoints

## Commands Run
- `cargo fmt`
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `65 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
