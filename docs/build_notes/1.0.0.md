# Build Notes - v1.0.0

## Summary
- Bumped crate version from `0.33.0` to `1.0.0`.
- Added GitHub Actions release workflow:
  - File: `.github/workflows/release.yml`
  - Trigger: tag pushes matching `v*`
  - Guard: verifies tagged commit is on `master`
  - Build: runs `./scripts/build_wasm.sh` on Linux
  - Publish: zips `dist/` and uploads it to GitHub Release assets

## Commands Run
- `./scripts/build_wasm.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
