# Build Notes - v2.28.0

## Summary
- Bumped crate version from `2.27.0` to `2.28.0`.
- Updated stack rendering so the top-of-stack stays visually pinned to the bottom row.
- Empty frame rows now fill from the top when stack depth is less than the visible frame count.
- Maintained fixed-size stack panel behavior from `Visible` frame count.

## Commands Run
- `cargo test`
- `./scripts/build_wasm.sh`

## Build Result
- Tests: `82 passed, 0 failed`.
- WASM release build: success.
- Dist artifacts refreshed (`dist/Post_Fix_Pro.html`, `dist/Post_Fix_Pro.png`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
