# Build Notes - v0.9.0

## Summary
- Bumped crate minor version from `0.8.0` to `0.9.0`.
- Added explicit wasm build command path via `scripts/build_wasm.sh`.
- Added matrix payload support in Rust API/WASM boundary (`push_matrix_json`).
- Implemented matrix A/B UI input flow in `index.html` with push actions and live solve wiring.
- Replaced placeholder solve behavior with real `solve_ax_b` invocation.
- Added `docs/wasm_build.md` with build usage guidance.
- Ran tests and release build.
- Copied `index.html` to `dist/index.html`.

## Commands Run
- `cargo test`
- `cargo build --release`
- `cp index.html dist/index.html`

## Build Result
- Tests: passed (`29` tests total)
- Release build: success

## Scope Added
- Rust API:
  - `MatrixInput` DTO
  - `CalculatorApi::push_matrix`
  - wasm `WasmCalculator::push_matrix_json`
- Frontend:
  - Matrix A/B text inputs and push buttons
  - Solve button wired to `solve_ax_b`
  - Matrix text parser (rows by newline, values by spaces/commas)
  - Improved runtime hint to reference repo script path
- Tooling/docs:
  - `scripts/build_wasm.sh` (wasm-pack wrapper)
  - `docs/wasm_build.md`
