# Build Notes - v0.24.0

## Summary
- Bumped crate minor version from `0.23.0` to `0.24.0`.
- Reorganized scientific UI into separate top-level panels:
  - `Scalar`
  - `Trigonometry`
  - `Complex`
  - `Number Theory`
  - `Rounding`
  - `Constants`
- Added an `Other` panel for tools that do not fit those categories:
  - Complex entry inputs / `Push a+bi`
  - Memory controls (`STO`, `RCL`, `CLR`)
- Updated scientific action event delegation to support multiple category panels while preserving existing action mappings.

## Commands Run
- `./scripts/build_wasm.sh`

## Build Result
- WASM release build: success.
- Dist artifacts refreshed (`dist/index.html`, `dist/help.md`, `dist/pkg`, `dist/wasm_base64.js`).
