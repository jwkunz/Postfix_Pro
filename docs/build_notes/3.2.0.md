# Build Notes 3.2.0

## Summary

- Fixed tooltip parsing to support multiple backticked keys on one help line.
- Added missing tooltip key mapping for `log-y-x`.
- Disambiguated gamma tooltips:
  - `gamma` remains the special function tooltip.
  - Constant gamma now uses `γ`.
- Added explicit `Apply` tooltip entry in `help.md`.
- Renamed panel label to `Special`.
- Removed legacy `std_dev` button/API path (using `std_dev_p` and `std_dev_s` only).

## Validation

- `cargo test`
- `./scripts/build_wasm.sh`

